<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.inacap.puntaarenas.evaluacion2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="Inicio de sesion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Seccion clientes" />
        <activity
            android:name=".Pedidos"
            android:exported="true"
            android:label="Seccion Pedidos y Productos" />
        <activity android:name=".Montos" />
        <activity
            android:name=".ListaProductos"
            android:label="Lista de Productos" />
    </application>

</manifest>